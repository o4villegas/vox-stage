# Handoff — from the cloud session to a local Claude Code session (2026-09-03)

Read this file first, then `CLAUDE.md` in full. `CLAUDE.md` is the project's memory; this
file is the bridge from the previous session to you.

## 0. Who you are working for, and the rules that do not bend

Lando is the owner and is **not technically savvy** — explain in plain language, one idea
per sentence, and ask questions as A/B/C choices with a recommendation. The five working
rules at the top of `CLAUDE.md` are binding. The one that matters most today: **rule 1 —
no application code without explicit approval**. That approval has now been given, with a
defined scope (below). Do not widen it.

## 1. What has been decided (all by Lando, verbatim "option 2, melody grade yes")

| Decision | Status | Where recorded |
|---|---|---|
| **M1 approved** | YES, 2026-09-03 | `CLAUDE.md` current-state; `docs/STATUS-2026-09-01.md` §6 item 3 |
| M1 scope | **Option 2 of `docs/STATUS-2026-09-01.md` §8**: commit the verified skeleton as the first M1 commit, then continue the full M1 build per `docs/M1-PLAN.md` in the same draft PR | §8 |
| Deploy path | **Cloudflare Workers Builds** on the existing Worker `voxstage-staging` (created 2026-09-01 20:50 UTC, Git-connected to this repo, production branch `main`). No API token needed. Non-`main` branches build with `wrangler versions upload`; `main` deploys with `wrangler deploy` | `docs/M1-PLAN.md` §6 row 3; R59 |
| S3 melody gates | **Ratified** (octave-err ≤ 5 %, median ≤ 25 ¢, voicing ≥ 85 %) → S3 CLOSED | `spikes/README.md` |
| Frontend / email / naming / lint | React 18 + Vite · Resend · `voxstage` + `voxstage-staging` · Biome | `docs/M1-PLAN.md` §6 |

## 2. What is still OPEN (ask Lando in plain language; do not decide for him)

1. **S2 endpoint** (last Phase 0 item; cold-start unmeasured). Three paths in
   `docs/STATUS-2026-09-01.md` §4. You have more access than the cloud session had:
   Docker Desktop on Lando's machine (if he starts it) and direct Cloudflare/RunPod reach.
   Path A (build + push `spikes/s2-runpod-worker/Dockerfile` to Docker Hub as `gvo555`,
   then `PATCH` the existing RunPod endpoint `pw1wbkuc138zz4` / template `e26f2wqimu`,
   container disk 30 GB, `workersMax ≥ 1`, run `spikes/s2-runpod-worker/bench.mjs`) is
   fully scriptable from a machine with Docker. **Never push the sandbox-overlay image
   (R53) — build clean.** Delete/scale the endpoint to `workersMax: 0` after the benchmark.
2. **Resend sending domain** for OTP email (none chosen). Until then the M1 dev flag
   `AUTH_DEV_ECHO=1` (M1-PLAN §3) is the test path.

## 3. Repo state you inherit

- `main` = `67a5313`. **Draft PR #11** (branch `claude/project-status-mvp-assessment-tgynqv`)
  holds the status report, R59, this handoff, and the decisions above. **Either Lando
  merges PR #11 first (recommended, one click), or you `git fetch origin
  claude/project-status-mvp-assessment-tgynqv` and read the docs from that branch.**
  If it is not merged, start your M1 branch from it, not from `main`, so `CLAUDE.md`
  carries the decisions.
- No application code exists anywhere. The only CI is `.github/workflows/spike-pages.yml`.
- Every push to any branch triggers the `voxstage-staging` Workers Build. It currently
  fails with `Missing entry-point to Worker script or to assets directory` — verified root
  cause in `docs/STATUS-2026-09-01.md` §8 — and turns green the moment the skeleton lands.
- The `voxstage-spikes` **Pages** project is also Git-connected; its automatic deployments
  must stay OFF or the phone test-page mirror breaks (R59). Not your deploy target.

## 4. Environment facts that bit the last session (R59 — do not re-learn them)

- The cloud sandbox could not reach `api.cloudflare.com` or `api.resend.com`; **you can**
  (local machine, no egress wall). `wrangler` there is OAuth-logged-in as
  lando555@gmail.com with `workers`, `d1`, `queues`, `pages` write scopes.
- Env vars in the cloud environment: `CLOUDFLARE_API_TOKEN` (`cfat_` Account API Token,
  scopes never verified), `RESEND_API_KEY` (unverified), `DOCKER_API_KEY` (unverified),
  `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` (unverified; probably R2 S3 keys),
  `S3_API_KEY` = the R2 S3 endpoint URL (misnamed). Verify each before relying on it,
  and never print a secret.
- Interactive `wrangler deploy` at repo root auto-detects "Framework: Static, Output
  Directory: spikes" and offers to write a config — **never accept that prompt**.
- Cloudflare's official Claude Code plugin (`claude plugin marketplace add
  cloudflare/skills && claude plugin install cloudflare@cloudflare`) was useful; install it
  locally if you want the wrangler/Workers skills.
- Lando's global `~/.claude/CLAUDE.md` on that machine also applies to you. If it
  conflicts with the repo `CLAUDE.md`, the repo rules win for this project; say so if you
  hit a conflict.

## 5. Your first hour, in order

1. Read `CLAUDE.md`, `docs/M1-PLAN.md`, `docs/STATUS-2026-09-01.md` §8, and
   `docs/ARCHITECTURE.md` §4 and §6.
2. Branch `claude/vox-stage-m1` (from PR #11's branch if unmerged). Commit the skeleton
   exactly as §8 describes; `npx wrangler versions upload --dry-run` must pass locally
   before you push. Push; open a **draft PR** into `main`; confirm the Workers Build check
   goes green.
3. Continue M1 per `docs/M1-PLAN.md`: Vite + React 18 SPA in `app/`, Hono Worker in
   `worker/`, D1 migrations (`users`, `otp_codes`, `sessions`), email-OTP endpoints, Biome,
   vitest with `@cloudflare/vitest-pool-workers`, GitHub Actions CI (lint/typecheck/test
   only — deploy is Cloudflare's job). Re-verify each dependency's current version against
   npm before pinning (rule 3; the plan's versions are from 2026-08-29).
4. "Done" for M1 = Lando signs in on his phone at the `voxstage-staging` URL. Ask him to
   test; record the result in `docs/RESEARCH.md` as a new R-entry and update
   `CLAUDE.md` current-state.
5. Every new verified fact → `docs/RESEARCH.md` with source and date. Every decision that
   contradicts an Accepted ADR → a new superseding ADR, never a silent change.

## 6. Things the previous session promised Lando that you now own

- The **Phase 0 exit report** (short doc: S0–S4 outcomes, gates, residual risks). Write it
  once S2's cold-start is measured, or write it now with S2 marked "2 of 3 gates, cold-start
  pending" if Lando prefers not to wait.
- Rotating the Hugging Face tokens exposed in plain `env` on two unrelated RunPod endpoints
  (`smokescan-analysis`, `DocuDamage`) — optional hygiene, flag it to him once.

## 7. Outcome of the first local session (2026-09-03, appended by that session)

§5 steps 1–3 are done: branch `claude/vox-stage-m1` from PR #11's branch, skeleton commit
`2f8dda4` turned the Workers Builds check green (build `27fc48c8`), draft **PR #12**
opened, then the full M1 scaffold (`307b163`) with 27 passing tests and green CI, verified
end-to-end on the branch preview (R60, R61). Step 4 — Lando signing in on his phone —
needs his `RESEND_API_KEY` secret in the dashboard (the test sender only reaches the
Resend account owner's inbox) and, optionally, the dashboard Build command that retires
the `postinstall` bridge. `CLAUDE.md` current-state has the exact clicks.

